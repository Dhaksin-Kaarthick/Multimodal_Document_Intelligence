version: '3.8'

services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    container_name: doc_intelligence_backend
    ports:
      - "8000:8000"
    volumes:
      - ./Backend/uploads:/app/uploads
      - backend_data_vol:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - OLLAMA_HOST=http://host.docker.internal:11434
      # ADD THIS LINE: Pass the hidden host key securely into the Docker backend container env
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY} 
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  frontend:
    image: nginx:alpine
    container_name: doc_intelligence_frontend
    ports:
      - "3000:80"
    volumes:
      - ./Frontend:/usr/share/nginx/html
    command: /bin/sh -c "sed -i 's/index  index.html index.htm;/index  login.html index.html;/' /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
    restart: unless-stopped

volumes:
  backend_data_vol: {}